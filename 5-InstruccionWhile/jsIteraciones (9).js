function mostrar()
{

	var contador=0;
	var maximo=10;
	var minimo=1;
	var numeroIngresado;

	// declarar variables
	
	var respuesta='si';

	while(respuesta!='no')
	{
		contador=contador+1;
		
		if(maximo<10)
		{
			maximo=10

		}

		if(minimo>10)
		{
			minimo=1

		}

		numeroIngresado=prompt("Ingrese su Numero");
		numeroIngresado=parseInt(numeroIngresado);
	}




}//FIN DE LA FUNCIÓN